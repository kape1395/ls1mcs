{application, ls1mcs, [
    {description, "LituanicaSat-1 MCS"},
    {vsn, git},
    {registered, []},
    {mod, {ls1mcs_app, []}},
    {applications, [
        kernel,
        stdlib,
        dthread,
        uart,
        gproc
    ]}
]}.
